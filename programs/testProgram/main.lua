-- basicFunctions.drawWindow("My program test")

-- while true do
--     --events, kernelData = coroutine.yield()
--     local data = table.pack(coroutine.yield())

--     --term.clear()
--     term.setTextColor(32768)
--     term.setCursorPos(1,2)
--     for i = 1, 7 do
--         term.setCursorPos(1, 2 + i)
--         term.write(data[i])
--     end
-- end

-- term.setBackgroundColor(1)
-- term.clearLine()
-- while true do
--     local event, button, X, Y = os.pullEventRaw()
-- end


-- shell.run("/graphicalOS_data/user_data/npaintpro.lua -a testme")
shell.run("shell")